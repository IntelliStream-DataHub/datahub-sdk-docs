page = "docs/industries/transport-logistics/retail.mdx"

# Blocks on this page:
#   python #1   L23    Set up demo data
#   java   #1   L44    1. A dataset per region
#   python #2   L61    1. A dataset per region
#   rust   #1   L75    1. A dataset per region
#   java   #2   L103   2. Roll up demand per region
#   python #3   L112   2. Roll up demand per region
#   rust   #2   L119   2. Roll up demand per region
#   python #4   L138   See the result

[blocks]
python = 4
java = 2
rust = 2

[python]
# The demo block and step 1 create the same nodes, and a reader does one or the other. Here the
# demo block is what runs: it seeds the rest of the graph and the datapoints the later blocks
# read, which step 1 alone does not. Step 1's own call is the same shape, and is compiled.
exclude = [2]
timeout = 600

[owns]
datasets = ["region_nordics"]
timeseries = ["store_oslo_01_sku_4471_sales"]

[expect]
datasets = ["region_nordics"]
timeseries = ["store_oslo_01_sku_4471_sales"]
