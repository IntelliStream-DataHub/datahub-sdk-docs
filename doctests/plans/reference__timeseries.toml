page = "docs/reference/timeseries.md"

# Blocks on this page:
#   java   #1   L21    Create a series
#   python #1   L33    Create a series
#   rust   #1   L47    Create a series
#   java   #2   L80    Value types
#   python #2   L93    Value types
#   rust   #2   L102   Value types
#   java   #3   L131   Filter series
#   python #3   L146   Filter series
#   rust   #3   L158   Filter series
#   java   #4   L184   Delete a series
#   python #4   L193   Delete a series
#   rust   #4   L200   Delete a series
#   java   #5   L220   Write datapoints
#   python #5   L234   Write datapoints
#   rust   #5   L246   Write datapoints
#   java   #6   L274   High-throughput ingestion
#   python #6   L290   High-throughput ingestion
#   rust   #6   L304   High-throughput ingestion
#   java   #7   L326   Retrieve datapoints
#   python #7   L346   Retrieve datapoints
#   rust   #7   L363   Retrieve datapoints
#   java   #8   L401   IngestResult
#   java   #9   L412   IngestResult

[blocks]
python = 7
java = 9
rust = 7

[python]
timeout = 600
# An API reference, not a tutorial: each fence is a self-contained example, and they
# are meant to be read independently. Running them as one program would invent
# conflicts a reader never meets — the delete example removing what the write example
# needs — so each block runs on its own.
independent = true

# Each example needs a client. Composing the quickstart in to get one would also
# create its `engine_temperature`, which several of these pages create themselves;
# `from_env()` is the constructor this section documents, so this is the reader's
# own call.
prologue = """
import intellistream_datahub_sdk
client = intellistream_datahub_sdk.DataHubClient.from_env()
"""

[java]
disabled = "Java scenario not written yet."

[rust]
disabled = "Rust scenario not written yet."

[owns]
timeseries = ["book_value_usd", "engine_temperature"]

[expect]
timeseries = ["book_value_usd", "engine_temperature"]
