page = "docs/guides/work-with-units.mdx"

# python #1 L28  — Browse the catalogue
# python #2 L62  — Tag a series with a unit
# python #3 L103 — Look up a specific unit
[blocks]
python = 3
java = 3
rust = 3

[python]
requires = ["quickstart"]
timeout = 120

[java]
disabled = "Java scenario not written yet."

[rust]
disabled = "Rust scenario not written yet."

[owns]
timeseries = ["engine_temperature", "wellhead_pressure_bar"]

[expect]
timeseries = ["wellhead_pressure_bar"]
