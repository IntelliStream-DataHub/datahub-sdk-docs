page = "docs/industries/manufacturing-process/semiconductor.mdx"

# Blocks on this page:
#   python #1   L26    Set up demo data
#   java   #1   L49    1. Record process data exactly
#   python #2   L64    1. Record process data exactly
#   rust   #1   L78    1. Record process data exactly
#   java   #2   L105   2. Find the tool every failing lot shares
#   python #3   L120   2. Find the tool every failing lot shares
#   rust   #2   L132   2. Find the tool every failing lot shares

[blocks]
python = 3
java = 2
rust = 2

[python]
timeout = 600

[java]
disabled = "Java scenario not written yet."

[rust]
disabled = "Rust scenario not written yet."

# The page models the network and traverses it in the next breath. The graph read
# path trails writes by a second or two, and an empty neighbourhood is a valid
# answer rather than an error — so without this the page prints the wrong
# conclusion and the test cannot tell that from a real one.
[[python.inject]]
before = 3
code = """
from tutorial_support import wait_for_related
wait_for_related(client, "lot_22841")
"""

[owns]
resources = ["lot_22841", "lot_22863", "tool_cmp_05", "tool_etch_07", "tool_litho_03"]
timeseries = ["tool_etch_07_chamber_pressure_mtorr"]

[expect]
resources = ["lot_22841", "lot_22863", "tool_cmp_05", "tool_etch_07", "tool_litho_03"]
timeseries = ["tool_etch_07_chamber_pressure_mtorr"]
